# Deep Agents UI - Environment Variables Configuration
# Copy this file to .env.local and configure the values for your environment

# =============================================================================
# CORE CONFIGURATION (Required for basic functionality)
# =============================================================================

# LangGraph server URL - The backend server URL where your agent is deployed
VITE_DEPLOYMENT_URL="http://127.0.0.1:2024"

# Agent identifier - Must match your agent ID from langgraph.json
VITE_AGENT_ID="deepagent"

# Workspace identifier - Required for agent run endpoints
VITE_WORKSPACE_ID="default"

# LangSmith API key - Required for authentication and monitoring
# Get this from your LangSmith dashboard: https://smith.langchain.com
VITE_LANGSMITH_API_KEY=""

# =============================================================================
# AUTH0 AUTHENTICATION (Optional - for production environments)
# =============================================================================

# Auth0 tenant domain - Your Auth0 domain (e.g., your-tenant.auth0.com)
# If not configured, the app will use development mode with VITE_LANGSMITH_API_KEY
VITE_AUTH0_DOMAIN=""

# Auth0 application client ID - From your Auth0 Single Page Application settings
VITE_AUTH0_CLIENT_ID=""

# Auth0 API audience - Your API identifier from Auth0 (optional)
# Set this if you need to validate tokens on your backend
VITE_AUTH0_AUDIENCE=""

# =============================================================================
# FEATURE FLAGS (Optional)
# =============================================================================

# Enable AI SDK enhanced features - Set to "true" to enable in production
# Automatically enabled in development mode
# Provides enhanced error handling, retry logic, and better debugging
VITE_ENABLE_AI_SDK=""

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================

# 1. For local development:
#    - VITE_DEPLOYMENT_URL should point to your local LangGraph server
#    - VITE_LANGSMITH_API_KEY is required for authentication
#    - Auth0 variables are optional (app will use development fallback)

# 2. For production deployment:
#    - Configure all Auth0 variables for proper authentication
#    - Set VITE_DEPLOYMENT_URL to your production server
#    - VITE_LANGSMITH_API_KEY can be used as fallback authentication

# 3. Environment variable precedence:
#    - .env.local (highest priority, not committed to git)
#    - .env.production (production builds)
#    - .env.development (development builds)
#    - .env (lowest priority, committed to git if needed)

# 4. All VITE_ prefixed variables are exposed to the client-side code
#    - Do not put sensitive server-only secrets in VITE_ variables
#    - These are visible in the browser and build output